# Twitter API
tweepy==4.14.0          # cliente ligero y estable

# Manipulación de datos y escritura Parquet
pandas==2.2.2           # versión compatible con PyArrow ≥ 15
pyarrow==15.0.2         # compila wheels manylinux para Lambda

# SDK de AWS (ya viene en el runtime, pero incluimos la versión estable por si quieres fijarla)
boto3==1.34.60
